# =============================================================================
# AGENTE CIDADÃO - CONFIGURAÇÃO DE AMBIENTE
# =============================================================================
#
# Este arquivo contém todas as variáveis de ambiente necessárias para o frontend
# do Agente Cidadão. Copie este arquivo para .env e preencha com seus valores.
#
# IMPORTANTE: Nunca commite o arquivo .env com credenciais reais!
#
# =============================================================================

# -----------------------------------------------------------------------------
# N8N WEBHOOK CONFIGURATION
# -----------------------------------------------------------------------------
# URL do webhook n8n que processa as mensagens do chat
# Produção: https://n8n-agentecidadaoagentico-production.up.railway.app/webhook/chat
# Desenvolvimento: Use sua instância local ou de staging do n8n
VITE_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Configurações do Supabase para banco de dados e real-time subscriptions
# Obtenha estas credenciais no painel do Supabase: Settings > API

# URL do seu projeto Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co

# Chave anônima (pública) do Supabase - seguro para uso no frontend
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# -----------------------------------------------------------------------------
# FEATURES FLAGS
# -----------------------------------------------------------------------------
# Habilita/desabilita funcionalidades específicas do sistema

# Ativa o sistema de memória conversacional (histórico de contexto)
# Valores: true | false
# Default: false
VITE_ENABLE_CONVERSATION_MEMORY=true

# Ativa o modo debug com logs detalhados no console
# Valores: true | false
# Default: false
VITE_DEBUG_MODE=false

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS
# -----------------------------------------------------------------------------

# Timeout para requisições ao webhook n8n (em milissegundos)
# Default: 360000 (6 minutos)
# VITE_REQUEST_TIMEOUT=360000

# Intervalo de polling para verificar status das requisições (em ms)
# Default: 1000 (1 segundo)
# VITE_POLLING_INTERVAL=1000

# Idioma padrão da interface
# Valores: pt-BR | es-ES
# Default: pt-BR
# VITE_DEFAULT_LANGUAGE=pt-BR

# -----------------------------------------------------------------------------
# ANALYTICS E MONITORAMENTO (OPCIONAL)
# -----------------------------------------------------------------------------

# Google Analytics ID (se aplicável)
# VITE_GA_ID=G-XXXXXXXXXX

# Sentry DSN para rastreamento de erros (se aplicável)
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# -----------------------------------------------------------------------------
# NOTAS DE CONFIGURAÇÃO
# -----------------------------------------------------------------------------
#
# 1. Para desenvolvimento local:
#    - Configure um projeto Supabase local ou use o projeto de desenvolvimento
#    - Execute n8n localmente ou use instância de staging
#
# 2. Para produção:
#    - Use as credenciais do projeto Supabase de produção
#    - Aponte para o webhook n8n de produção no Railway
#    - Ative VITE_ENABLE_CONVERSATION_MEMORY=true
#
# 3. Segurança:
#    - NUNCA exponha SUPABASE_SERVICE_KEY no frontend
#    - Use apenas SUPABASE_ANON_KEY (chave pública)
#    - Configure Row Level Security (RLS) no Supabase
#
# =============================================================================
